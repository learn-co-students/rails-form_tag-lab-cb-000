<div>
  <% if !@students.empty? %>
    <% @students.each do |student| %>
      <div><%= link_to student.to_s, student_path(student) %></div>
    <% end %>
  <% else %>
      <p>Student list is empty</p> <br> 
  <% end %>
</div>
